# vehicle-edge-c_car_platform

Vehicle/edge component container (placeholder).

## Local development

### Environment
Copy and edit:
- `.env.example` -> `.env`

### Commands (planned)
Depending on eventual implementation (Node.js, Python, or other), add consistent scripts:
- `lint`, `format`, `test`
